
export enum UserType {
    Admin = "admin",
    Vendor = "vendor",
    Customer = "customer",
};

export enum OTPType {
    Reset = "passwordReset",
    Verification = "emailVerification"
};

export enum CategoryType {
    Main = "category",
    SubMain = "subCategory",
    Sub = "subSubCategory"
};