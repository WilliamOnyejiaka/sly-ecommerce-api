
export enum UserType {
    Admin = "admin",
    Vendor = "vendor",
    Customer = "customer",
};

export enum CategoryType {
    Main = "category",
    SubMain = "subCategory",
    Sub = "subSubCategory"
};